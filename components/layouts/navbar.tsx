import React from "react";

const Navbar = () => {
  return (
    <nav className="bg-accent/1 py-4 lg:fixed w-full border-b border-b-accent">
      <div className="container">Navbar</div>
    </nav>
  );
};

export default Navbar;
